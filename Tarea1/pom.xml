<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>uy.fing.tprog</groupId>
    <artifactId>tpgr27-parent</artifactId>
    <version>1.0.0</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>servidor-central</artifactId>
  <packaging>jar</packaging>
  <name>servidor-central</name>

  <build>
    <!-- Como tus fuentes NO están en src/main/java, configuramos el directorio -->
    <sourceDirectory>${project.basedir}/src.ServidorCentral</sourceDirectory>

    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.source}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- Agrega acá dependencias que tu lógica necesite (si las hubiera) -->
  </dependencies>
</project>

